You are working inside the Replit project “PropertyAssistant”.

---

## OBJECTIVE
Rebuild the top section of the Documents page ("/documents") to match the premium “Documentation” layout shown in the screenshot.  
The style should be clean, structured, and minimalist — similar to Apple Support or Notion.

---

## TASKS

### 1️⃣ HEADER SECTION
- Replace the current top heading layout with this structure:
  ```tsx
  export default function DocumentsPage() {
    const tags = [
      "Handover",
      "Snagging",
      "Warranties",
      "Appliance manuals",
      "Waste & recycling",
      "Parking rules",
      "Fire safety",
      "Floorplans"
    ];

    return (
      <section className="h-full w-full bg-white dark:bg-neutral-950 text-gray-900 dark:text-gray-100 overflow-hidden">
        {/* Header */}
        <div className="max-w-6xl mx-auto px-6 pt-[calc(var(--topbar)+2rem)] pb-6">
          <div className="flex items-center gap-3 mb-2">
            <div className="text-blue-700 dark:text-blue-400 text-2xl">
              📄
            </div>
            <h1 className="text-3xl font-semibold">Documentation</h1>
          </div>
          <p className="text-gray-500 dark:text-gray-400 text-base mb-8">
            Everything you need for handover and day-to-day living.
          </p>

          {/* Search Box */}
          <div className="flex items-center bg-white dark:bg-neutral-900 border border-gray-200 dark:border-neutral-800 rounded-full shadow-sm px-4 py-3 max-w-4xl mx-auto mb-8">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" strokeWidth="1.5" stroke="currentColor" className="w-5 h-5 text-gray-400 dark:text-gray-500 mr-2">
              <path strokeLinecap="round" strokeLinejoin="round" d="M21 21l-4.35-4.35m0 0A7.5 7.5 0 104.5 4.5a7.5 7.5 0 0012.15 12.15z" />
            </svg>
            <input
              type="text"
              placeholder="Search manuals, certificates, rules..."
              className="flex-1 bg-transparent outline-none text-sm md:text-base placeholder-gray-400 dark:placeholder-gray-500 text-gray-800 dark:text-gray-100"
            />
          </div>

          {/* Tag Buttons */}
          <div className="flex flex-wrap justify-start gap-3">
            {tags.map((tag) => (
              <button
                key={tag}
                className="
                  px-5 py-2 text-sm rounded-full border border-gray-200 dark:border-neutral-800
                  bg-white dark:bg-neutral-900 hover:bg-gray-100 dark:hover:bg-neutral-800
                  text-gray-700 dark:text-gray-200 transition
                "
              >
                {tag}
              </button>
            ))}
          </div>
        </div>

        {/* Content List (placeholder) */}
        <div className="max-w-6xl mx-auto px-6 pb-[calc(var(--dock)+2rem)]">
          {/* Document cards or list items here */}
        </div>
      </section>
    );
  }
2️⃣ VISUAL STYLE PRINCIPLES
Use Inter or SF Pro typography feel — light weight, tight letter spacing.

All borders: border-gray-200 (light) / border-neutral-800 (dark).

Keep spacing generous (64–80px from top, 32px between sections).

Buttons should feel soft and tactile, no shadows — think flat, modern design.

Retain dark mode color parity (no gray tints shifting mid-tone).

3️⃣ THEME INTEGRATION
Ensure global variables --topbar and --dock are respected for vertical spacing.

Background adapts to light/dark with transitions:

css
Copy code
transition: background-color 0.3s ease, color 0.3s ease;
Use consistent padding: px-6, max-w-6xl mx-auto.

4️⃣ QA CHECK
✅ “Documentation” title left-aligned with 📄 icon.
✅ Subtitle gray, full width.
✅ Search bar centered and clean with icon.
✅ Category buttons evenly spaced, hover softly.
✅ Works seamlessly in both dark and light modes.
✅ Maintains no-scroll baseline (content fits between TopBar and Dock).