You are working in the PropertyAssistant monorepo.

OBJECTIVE:
Fix the mobile chat layout so it behaves exactly like ChatGPT / iMessage:
- The chat input bar must ALWAYS sit directly above the bottom navigation bar
- The gap between input and nav must be constant at all times
- No layout changes when keyboard opens or closes
- The user's last message and the assistant reply must always be visible
- The input bar and disclaimer must NEVER disappear off-screen

CRITICAL RULES:
- DO NOT calculate or hardcode the bottom nav height
- DO NOT use JS viewport math
- DO NOT rely on padding guesses
- DO NOT change TenantBottomNav height or styles
- Fix this purely via layout structure and CSS positioning

IMPLEMENTATION PLAN:

1. In `apps/unified-portal/components/purchaser/PurchaserChatTab.tsx`:
   - Set the root container to `h-[100dvh] flex flex-col`
   - The messages list must be `flex-1 overflow-y-auto`
   - Apply `padding-bottom` to the messages list equal to the input bar height using a CSS variable

2. Create a ChatInput container that is:
   - `position: sticky`
   - `bottom: env(safe-area-inset-bottom)`
   - `z-index` lower than the nav but above messages
   - Contains:
     - input field
     - send button
     - disclaimer text

3. The input bar must:
   - Have ZERO bottom margin
   - Have ZERO gap below the disclaimer
   - Sit flush against the bottom nav visually
   - Never move when keyboard opens

4. The bottom nav (`TenantBottomNav`) must remain:
   - `position: fixed`
   - untouched

5. Ensure:
   - Messages scroll UNDER the input bar
   - Only the message list scrolls, not the page
   - No conditional rendering removes the input bar

6. Remove any previous:
   - height calculations
   - resize listeners
   - keyboard event hacks
   - extra wrappers affecting vertical flow

SUCCESS CRITERIA:
- Chat screen always looks identical on load
- Keyboard open does not introduce new gaps
- Input bar never floats or detaches
- The exact same gap is visible at all times
- Behavior matches ChatGPT mobile UI

Do not return partial fixes. Implement the full layout correction in one pass.
