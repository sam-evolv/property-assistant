You are working inside the Replit project “PropertyAssistant”.

---

## OBJECTIVE
Rebuild the homepage layout to perfectly match the premium Lovable-style Seaview prototype.  
The design must be elegant, centered, responsive, and visually high quality.  
Place the Chat, Map, Noticeboard, and Documents buttons directly under the main search bar in a simple, modern, premium layout.

---

## TASKS

1. ✅ **Update `app/page.tsx`**
   - Replace the current content with the following complete implementation:

   ```tsx
   export default function Home() {
     return (
       <main className="min-h-screen flex flex-col items-center justify-center bg-white text-center px-6 font-sans fade-in">
         {/* Logo */}
         <div className="mb-8">
           <img
             src="/seaview-logo.svg"
             alt="Seaview Property Assistant"
             className="h-24 mx-auto drop-shadow-sm"
           />
           <p className="text-xs text-gray-500 tracking-widest mt-2 uppercase">
             Property Development — Cork, Ireland
           </p>
         </div>

         {/* Headline */}
         <h1 className="text-3xl md:text-5xl font-bold text-gray-900 leading-tight mb-4">
           Ask anything about your home or community
         </h1>
         <p className="text-gray-500 max-w-2xl text-base md:text-lg mb-10">
           Quick answers for daily life at Seaview — floor plans, amenities, local services, and more.
         </p>

         {/* Quick topic buttons */}
         <div className="flex flex-wrap justify-center gap-3 mb-10">
           {["Public Transport", "Waste collection", "Parking rules", "Local area"].map((item) => (
             <button
               key={item}
               className="px-5 py-2 bg-gray-100 hover:bg-blue-100 text-gray-700 rounded-full text-sm font-medium transition-all duration-200 shadow-sm hover:shadow-md"
             >
               {item}
             </button>
           ))}
         </div>

         {/* Search / Chat Bar */}
         <div className="w-full max-w-2xl flex items-center bg-gray-50 border border-gray-200 rounded-full shadow-sm hover:shadow-md transition-all duration-200 px-4 py-3 mb-8">
           <button className="text-blue-600 text-lg px-2">
             <i className="fa-solid fa-microphone"></i>
           </button>
           <input
             type="text"
             placeholder="Ask about your home or community..."
             className="flex-grow bg-transparent border-none outline-none px-3 text-gray-700 placeholder-gray-400"
           />
           <button className="bg-blue-600 hover:bg-blue-700 text-white rounded-full px-5 py-2 text-sm font-semibold transition-all duration-200">
             Ask
           </button>
         </div>

         {/* Navigation Cards */}
         <div className="flex flex-wrap justify-center gap-5 w-full max-w-lg">
           {[
             { name: "Chat", icon: "💬", href: "/chat" },
             { name: "Map", icon: "🗺️", href: "/map" },
             { name: "Noticeboard", icon: "📢", href: "/noticeboard" },
             { name: "Documents", icon: "📄", href: "/documents" },
           ].map((item) => (
             <a
               key={item.name}
               href={item.href}
               className="flex flex-col items-center justify-center w-28 h-24 rounded-2xl bg-white border border-gray-200 shadow-sm hover:shadow-md hover:border-blue-300 transition-all duration-200"
             >
               <span className="text-2xl mb-1">{item.icon}</span>
               <span className="text-sm font-medium text-gray-700">{item.name}</span>
             </a>
           ))}
         </div>
       </main>
     );
   }
✅ Enhance tailwind.config.ts
Add this to theme.extend for brand consistency and subtle shadows:

ts
Copy code
extend: {
  fontFamily: {
    sans: ["Inter", "system-ui", "sans-serif"],
  },
  colors: {
    brand: {
      blue: "#1A3E75",
      gold: "#E2B652",
    },
  },
  boxShadow: {
    sm: "0 1px 2px rgba(0,0,0,0.05)",
    md: "0 4px 8px rgba(0,0,0,0.08)",
  },
  keyframes: {
    fadeIn: { "0%": { opacity: 0 }, "100%": { opacity: 1 } },
  },
  animation: {
    fadeIn: "fadeIn 0.8s ease-in-out",
  },
},
✅ Add a small CSS helper (optional)
Create or append to globals.css:

css
Copy code
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
.fade-in { animation: fadeIn 0.7s ease-in-out; }
✅ Quality Checklist

Layout fully centered and consistent with prototype.

Buttons under the search bar in one neat row (wraps elegantly on mobile).

Subtle shadows and hover effects applied.

All typography and spacing consistent.

Works perfectly on mobile and desktop.

✅ Commit message

vbnet
Copy code
feat: rebuild homepage layout with Lovable-style design and new navigation buttons
ACCEPTANCE CRITERIA
Homepage matches the attached Seaview prototype exactly in structure and spacing.

Navigation cards (Chat, Map, Noticeboard, Documents) appear directly under the search bar in a minimal, polished style.

All elements scale smoothly between desktop and mobile.

Overall visual feel: premium, calm, professional, and balanced.